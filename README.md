# SuisenSha

Recommendation pipeline combining **embedding similarity**, **transformer sequential recommendation (SASRec)**, and **RAG-augmented LLM generation** on MovieLens-100K.

```bash
pip install -e .
python -m pipeline.cli
```

📖 [Documentation](docs/GUIDE.md)
